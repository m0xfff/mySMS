%nav.navbar.navbar-default.navbar-static-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{:type => "button", 'data-toggle' => "collapse", 'data-target' => "mysms-navbar", 'aria-expanded' => "false" }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{ :href => students_path } mySMS
    .collapse.navbar-collapse#mysms-navbar
      %ul.nav.navbar-nav
        %li{ class: current_page?(students_path) ? "active" : nil }
          = link_to 'Students', students_path
        %li{ class: current_page?(courses_path) ? "active": nil }
          = link_to 'Courses', courses_path, class: current_page?(courses_path) ? "active" : nil
