%h1 🏫 Institutes

%table.table
  %tr
    %th{scope: "col"} Name
    %th{scope: "col"}
  = render partial: 'shared/links', collection: @institutes, as: :instance
  = render partial: 'institute', collection: @institutes, as: :institute

.d-flex.justify-content-center= will_paginate @institutes
